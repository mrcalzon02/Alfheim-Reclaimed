// Alfheim Reclaimed — recipe integration baseline
// This event is intentionally empty until the unmodified mod stack has passed first-boot validation.
console.info('[Alfheim Reclaimed] Server recipe scaffold loaded.');

ServerEvents.recipes(event => {
    // Future recipe integration belongs here.
    // Do not remove vanilla/mod recipes until their intended replacement path exists and is tested.
});
